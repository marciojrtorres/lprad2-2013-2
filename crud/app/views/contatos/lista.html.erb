<h1>Lista de Contatos</h1>

<table>
    <thead>
        <tr><th>Nome</th><th>Telefone</th></tr>
    </thead>
    <tbody>
    <% for c in @contatos %>
        <tr>
            <td><%= c.nome %></td>
            <td><%= c.telefone %></td>
        </tr>
    <% end %>
    </tbody>
</table>
